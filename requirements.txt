# Training / generation environment (evaluation used Python 3.13; training code is compatible with Python 3.9+)
torch>=2.1
transformers>=4.40
peft>=0.10
datasets>=2.18
accelerate>=0.28
bitsandbytes>=0.43
vllm>=0.4
pyyaml
numpy
pytest
coverage

# Mutation testing: install in a separate Python 3.9 environment
# (pip install mutpy==0.6.1 in that environment; MutPy is incompatible with Python >= 3.10)
mutpy==0.6.1; python_version < "3.10"
